name: driver
description: Composed RokctAI app shell (installed by base_sdk).


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 2.8.9+390

environment:
  sdk: '>=3.5.0 <4.0.0'


dependencies:
  base_sdk:
    path: .rokct/cache/base
  auth_sdk:
    path: .rokct/cache/auth
  users_sdk:
    path: .rokct/cache/users
  orders_sdk:
    path: .rokct/cache/orders
  delivery_sdk:
    path: .rokct/cache/delivery
  comms_sdk:
    path: .rokct/cache/comms
  processing_sdk:
    path: .rokct/cache/processing
  calc_sdk:
    path: .rokct/cache/calc
  corporate_sdk:
    path: .rokct/cache/corporate
  map_sdk:
    path: .rokct/cache/map
  zones_sdk:
    path: .rokct/cache/zones
  revenue_sdk:
    path: .rokct/cache/revenue
  merchants_sdk:
    path: .rokct/cache/merchants
  products_sdk:
    path: .rokct/cache/products
  flutter:
    sdk: flutter
  uuid: ^4.4.0
  table_calendar: ^3.1.2

  #base:
  dio: ^5.7.0
  intl: ^0.19.0
  get_it: ^8.0.0
  google_fonts: 6.3.0
  pull_to_refresh: ^2.0.0
  flutter_screenutil: ^5.9.0
  proste_indexed_stack: ^0.2.4


  http: ^1.2.2
  crypto: ^3.0.6
  auto_route: ^9.2.0
  flutter_bloc: ^8.1.3
  path_provider: ^2.1.2
  flutter_riverpod: ^2.5.1
  freezed_annotation: ^2.4.4
  shared_preferences: ^2.5.2
  permission_handler: ^11.3.1
  flutter_displaymode: ^0.6.0

  #ui
  jiffy: ^6.2.2
  lottie: ^3.0.0
  confetti: ^0.8.0
  flutter_svg: ^2.0.9
  share_plus: ^10.1.1
  card_swiper: ^3.0.1
  sms_autofill: ^2.4.1
  blur: ^4.0.0
  palette_generator: ^0.3.3
  url_launcher: ^6.3.0
  flutter_remix: ^0.0.3
  webview_flutter: ^4.10.0
  intl_phone_field: ^3.2.0
  connectivity_plus: ^6.0.5
  sliding_up_panel: ^2.0.0+1
  flutter_rating_bar: ^4.0.1
  top_snackbar_flutter: ^3.1.0
  flutter_masked_text2: ^0.9.1
  flutter_html: ^3.0.0-alpha.6
  flutter_native_splash: ^2.4.2
  smooth_page_indicator: ^1.2.0
  visibility_detector: ^0.4.0+2
  flutter_swipe_detector: ^2.0.0
  flutter_advanced_switch: ^3.0.1
  flutter_carousel_slider: ^1.1.0
  floating_draggable_widget: ^2.1.5
  flutter_staggered_grid_view: ^0.7.0
  flutter_staggered_animations: ^1.1.1
  app_tracking_transparency: ^2.0.6
  remixicon: ^1.2.0
  super_tooltip: ^2.0.7
  shimmer: ^2.0.0 #Added
  package_info_plus: ^8.2.1
  device_info_plus: ^11.3.0
  battery_plus: ^4.0.0


  #media:
  image: ^4.1.7
  file_picker: ^8.0.7
  image_picker: ^1.0.7
  image_cropper: ^8.0.2
  cached_network_image: ^3.3.1

  #maps:
  geocoding: ^3.0.0
  geolocator: ^13.0.1
  google_place:
    git:
      url: https://github.com/Muhammadyunusxon/google_place.git
  osm_nominatim: ^3.0.1
  google_maps_flutter: ^2.9.0

  #auth:
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.2
  flutter_facebook_auth: ^7.0.0

  #firebase:
  firebase_dynamic_links: ^6.0.5
  firebase_messaging: ^15.1.0
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.0
  firebase_remote_config: ^5.1.3
  cloud_firestore: ^5.4.0

  payfast:
    git:
      url: https://github.com/RendaniSinyage/payfast_flutter.git

  # Template-host packages. The features that use these have finished their
  # move into SDK repos, but SDK templates are installed into the generated
  # lib/ at compose time and compile against THIS pubspec, so the entries stay
  # here by design (delivery_sdk's manifest documents them as host deps its
  # templates additionally need).
  #   workmanager           - delivery_sdk: courier_location_service lib code
  #                           + home_page template (also in its own pubspec)
  #   percent_indicator     - delivery_sdk: push-order countdown ring template
  #   map_launcher          - delivery_sdk: external-maps handoff templates
  #   auto_size_text        - delivery_sdk: order list typography templates
  #   calendar_date_picker2 - base_sdk dep; delivery_sdk custom_date_picker
  #                           template
  #   charts_flutter        - revenue_sdk income template builds its chart
  #                           Series host-side: revenue_sdk deliberately stays
  #                           chart-library-agnostic, so the host owns this dep
  # workmanager >=0.6 is the v2-embedding rewrite; 0.5.x still references the
  # deleted Flutter v1 shim (ShimPluginRegistry/PluginRegistrantCallback) and
  # fails :workmanager:compileReleaseKotlin on Flutter 3.29+.
  workmanager: ^0.10.7
  percent_indicator: ^4.2.2
  calendar_date_picker2: ^1.1.5
  map_launcher: ^3.3.1
  auto_size_text: ^3.0.0
  charts_flutter:
    git:
      url: https://github.com/Muhammadyunusxon/charts.git
      path: charts_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.5.7
  build_runner: ^2.4.12
  flutter_lints: ^5.0.0
  auto_route_generator: ^9.0.0
  flutter_launcher_icons: ^0.14.1
  cupertino_icons: ^1.0.6

flutter:

   uses-material-design: true
   assets:
    # BEGIN sdk-app-assets (generated by SDK installer - do not edit by hand)
    - assets/audio/notification.wav
    - assets/image/my_location.png
    - assets/images/app_logo.png
    - assets/images/card.png
    - assets/images/cardBg.png
    - assets/images/delivery.png
    - assets/images/door.png
    - assets/images/fast.png
    - assets/images/logo.png
    - assets/images/marker.png
    - assets/images/notFound.png
    - assets/images/order.png
    - assets/images/save.png
    - assets/images/set.png
    - assets/images/splash.png
    - assets/images/splash1.png
    - assets/images/splash2.png
    - assets/images/ui0.png
    - assets/images/ui1.png
    - assets/images/ui2.png
    - assets/images/ui3.png
    - assets/images/visa.png
    - assets/lottie/empty-box.json
    - assets/lottie/girl_empty.json
    - assets/lottie/not-found.json
    - assets/lottie/not_found_page.json
    - assets/lottie/notification_empty.json
    - assets/lottie/pin.json
    - assets/lottie/processing.json
    - assets/svg/balance.svg
    - assets/svgs/adress.svg
    - assets/svgs/avatar.svg
    - assets/svgs/brand_logo_rounded.svg
    - assets/svgs/contact.svg
    - assets/svgs/delivery.svg
    - assets/svgs/delivery2.svg
    - assets/svgs/delivery_time.svg
    - assets/svgs/discount.svg
    - assets/svgs/empty.svg
    - assets/svgs/filter.svg
    - assets/svgs/foodyman.svg
    - assets/svgs/menu.svg
    - assets/svgs/orderTime.svg
    - assets/svgs/pickUpFrom.svg
    - assets/svgs/star.svg
    - assets/svgs/ticket.svg
    # END sdk-app-assets
    - assets/images/
    - assets/svgs/
    - assets/lottie/
    - assets/
    - assets/temp/
    - assets/images/ui3.png
    - assets/images/splash.png  # Add this line to explicitly include splash.png
    - assets/images/splash1.png
    - assets/images/splash2.png
    - assets/images/intro/
    - assets/images/poi/
    - assets/images/water/
flutter_native_splash:
  background_image: 'assets/images/splash2.png'
  fullscreen: true
  android: true
  ios: true

flutter_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: 'assets/images/app_logo.png'

# Feature SDK pubspecs point at ../base (the composed-app cache layout).
# When paas_customer itself is the resolution root against sibling repos,
# this override redirects base_sdk to its real workspace location.
# Version arbitration across the composed SDK set (kernel pins an older
# ecosystem than the newest SDKs; root overrides win globally).
dependency_overrides:
  get_it: ^8.0.0
  intl: ^0.20.2
  share_plus: ^12.0.1
  freezed_annotation: ^2.4.4
  google_fonts: ^6.3.0
  http: ^1.2.0
  # drift must stay on a release whose sqlite3 constraint admits the pinned
  # sqlite3 2.9.4 below (2.34.x moved to sqlite3 ^3.x and fails to compile
  # against it). Mirrors paas_manager's and supacharge's arbitration of the
  # same conflict.
  drift: 2.31.0
  sqlite3: 2.9.4
  # workmanager must land on the v2-embedding rewrite (>=0.6): 0.5.x's Kotlin
  # references the Flutter v1 shim, which Flutter 3.29+ deleted, so
  # :workmanager:compileReleaseKotlin fails (Unresolved reference 'shim' /
  # 'ShimPluginRegistry'). The composed delivery_sdk cache may still pin
  # ^0.5.1 until its next compose picks up the zones-side bump; this override
  # keeps resolution on 0.10.x regardless of cache staleness.
  workmanager: ^0.10.7
