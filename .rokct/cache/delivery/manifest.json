{
  "name": "delivery_sdk",
  "version": "1.2.0",
  "home_page": "templates/pages/driver/home/home_page.dart",
  "installs": [
    {
      "from": "templates/assets",
      "to": "assets"
    }
  ],
  "app_assets": [
    "assets/images/marker.png",
    "assets/images/splash.png",
    "assets/images/splash1.png",
    "assets/images/splash2.png",
    "assets/lottie/empty-box.json"
  ],
  "_comment_app_assets": "Home SDKs ship the splash/login background images (base_sdk's splash_page and auth_sdk's login_page reference assets/images/splash*.png). Each home SDK provides its own copies so every composed app can brand them independently; app_assets gets them into the host pubspec's sdk-app-assets block so they are actually bundled. If base_sdk also installed identical files first, the file copy is skipped (already-present guard) and only the pubspec declaration matters. marker.png and empty-box.json are custodianship copies from paas_driver develop (foodyman-era driver assets evicted from base_sdk).",
  "routes": [],
  "integrations": []
}
